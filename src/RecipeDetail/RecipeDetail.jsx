import React from 'react';
import './recipeDetail.css';

const Card = ({ title, image, totalIngredients, ingredients }) => {
	return (
		<div>
			<h1>Im the Card</h1>
		</div>
	);
};

export default Card;
